
--To install ---
curl -SL https://github.com/docker/compose/releases/download/v2.15.1/docker-compose-linux-x86_64 -o /usr/local/bin/docker-compose

--To add permission--
sudo chmod +x /usr/local/bin/docker-compose
docker-compose -v

----Docker compose is a YML file which helps in running multiple container at a time.

docker-compose up -d  ---> run multiple containers using yaml file
docker-compose up -d -- scale webserver=2 --scale appserver=2 -d ---> run multiple containers using yaml file ( run 2 conmtainer)
docker compose down ----> deletes the container
